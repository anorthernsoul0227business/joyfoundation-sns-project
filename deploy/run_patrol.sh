#!/bin/bash
# 1日2回（朝7時・夕方18時）の見回り。
# 圭一郎さんのメモや直しの依頼を見つけ、解決案を添えて「やること」に積む。
# 承認されたものはここで実行する。
#
# 2026-09-08: それまでメモは書き込まれるだけで誰も見に行かず、
# 9/4 のメモが4日間放置されていた。見落とす人がいる限り
# 「圭一郎さんと AI で完結」にはならないので、見回りを仕組みにする。
set -u
cd "$(dirname "$0")"

# 先に承認ぶんを片づける。実行の結果が次の見回りに反映される
/usr/bin/python3 apply_tasks.py || echo "[warn] 承認ぶんの実行に失敗しました"
/usr/bin/python3 patrol.py      || echo "[warn] 見回りに失敗しました"
